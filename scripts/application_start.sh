#!/bin/bash

# session=$APPLICATION_NAME-$DEPLOYMENT_GROUP_NAME
# cmd="/home/ubuntu/ovs-admin-dashboard && docker-compose up -d --build"

# tmux kill-session -t "$session" || true
# tmux new -s "$session" -d
# tmux send -t "$session" "$cmd" ENTER;